# Archived session captures

This folder preserves dated runtime screenshots and their provenance. They are historical evidence
of particular builds and runs, not visual-style or implementation guidance. The reusable capture
workflow is the `session-captures` skill; dated subfolders contain media only unless a local
provenance note is necessary.
