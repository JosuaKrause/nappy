Use case: background-extraction. The input image is the edit target. Preserve exactly all eight rooftop object drawings in the 4-column two-row layout, every metal surface, highlight, glazing, tank support and local contact shadow. Replace the entire fake gray checkerboard and all its wavy/ghost outlines with a uniform pure white #FFFFFF background, also in every open gap between supports, branches, objects and edges. Do not repaint, resize, move, recolor or redesign any object; preserve the same image dimensions and cell placements. Remove only the checkerboard and its ghost patterns. Absolutely no checkerboard, gray square, mottling, labels, borders or transparency simulation. The output background must be flat untextured pure white, with precise dark ink outlines of the existing artwork intact.
