Use case: style-transfer.
Redraw the three SVG concepts in input 1 trees-atlas.png as comic game sprites. Input 1 is subject/pose/placement authority: one row of three separate centered cells, first a broad round-crowned green tree with visible trunk, second a narrower taller green tree with visible trunk, third a circular dark metal bollard cap seen DIRECTLY FROM ABOVE. Inputs 2 graphics-reference-urban-01.jpeg and 3 graphics-reference-cardinal.jpeg supply ONLY drawing style: articulate ink contours, warm muted colors, deliberate graphic shadow planes and sparse small-scale texture. Omit their UI and annotations.
Transfer the concept into authored comic drawing, not primitive circles with surface noise. Tree crowns need irregular leafy contours, a few broad layered clusters with ink shadows, olive/sage highlights upper left, dark green lower-right masses, real brown bark and sparse forked branches partly exposed beneath foliage. Preserve the first tree's broad crown versus second's narrower taller crown; keep their source proportions with long exposed trunks and source feet at bottom center. Each should be legible at only 40x46 and 40x52 native pixels. No scenery, root spread, ground mounds, shadows under trees (runtime supplies them), fruit, extra trees or props.
The third object is ONLY the top-down circular cap of a charcoal metal bollard, 12x12 native pixels: dark inked rim, graphite body, small left-upper gray highlight and restrained scuff. It MUST remain a flat top-down disc with no visible vertical post, base or side wall; round closed silhouette. Keep approximately the same relative scale as source inside the third cell.
Output exactly three independent drawings in a 3-column 1-row atlas on real RGBA transparency, ample blank gutters. No labels, grid, text or painted checkerboard. Same composition and source camera projections. Bold but elegant outlines and broad cel-shaded shapes, not a photographic or 3D render.
