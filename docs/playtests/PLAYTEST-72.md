# Playtest 72 — 2026-09-14

A phone session on the live page, v0.10.4 (1f80b57), opened with `?debug=1` and each of the
skip words in turn — the probe M124 asked for. The player sent three screenshots, then sixteen
more, and one instruction.

## The phone's process time, split

Nineteen screenshots of day 1 in `evidence/playtest-72-phone-skip-probe-2026-09-14/`, named by
what was skipped, where she stood and the seconds left: four with nothing skipped, three or four
each with `skip=shadows`, `skip=events` and `skip=crowd`, and three with all three words. Each
setting is its own page load and so its own seed; most were taken walking in the first thirty
seconds of the day, a few standing on the doorstep before the first step. The numbers off each
are tabled and read in `DECISIONS.md`, M124, the phone's process time split. The reading: the
draw calls are not the phone's cost, so the atlas item is struck, as the item said it would be
on that result.

For the `REVIEW.md` item that asked: the note reads `DEBUG MODE ON   v0.10.4 (1f80b57)` on the
phone — the newest tag and the commit it was built from — and the readout's `build` line repeats
it; with all three words the `skip` line sits under the seed, the street is bare and the meter
still charges. The readout stays readable with the extra line. The item closes.

## A seed on the live page

> "setting seed should be possible with debug=1"

> "that would keep the real game clean still"

Said when told that a seed cannot be fixed on the live page. Every setting above is a different
city, because each page load draws a fresh seed, and the readings vary with what that seed put
on screen more than with the setting. Filed as M138 in `TODO.md`: `?seed=N` honoured while
`?debug=1` holds, the way `?skip=` is, and never otherwise — the note that marks a debug page
is what keeps the real game clean.
