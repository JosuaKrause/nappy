# M160 provisional father B-contact exception

PLAYTEST-90 provisionally approves the four named father pushing B PNGs from
`docs/evidence/male-player-2026-09-19/b-contact/straight-contact-2026-09-19/generated/rig/`.
Their active manifest entries record each byte-identical runtime source and hash, alongside the
unchanged original registered PNG and hash. The verifier checks both relationships, so accepting
the runtime contact does not rewrite the earlier registered output or the rejected trial's frozen
inputs.

This is a player-specific M160 record, not a general walking-contact procedure or a raster
acceptance gate. It does not authorize hand painting, recoloring, reshaping, or combining leg
art in another correction. A further attempt to make these legs look more natural is a separate
candidate and requires visual review before it can replace the provisional contact.
